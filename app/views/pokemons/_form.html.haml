= form_for @pokemon do |f|
  - if @pokemon.errors.any?
    #error_explanation
      %h2= "#{pluralize(@pokemon.errors.count, "error")} prohibited this pokemon from being saved:"
      %ul
        - @pokemon.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
